#include "config.h"

int parse_config(config_t *config, int argc, const char **argv) {
    // TODO
    // Look through the command-line arguments and save all the options to config
    return 0;
}

int print_help(const char *argv0) {
    // TODO
    // Print the usage message to the console.  You're given argv[0], which is
    // the name of the executable (think "Usage: %s [options]").
    return 0;
}
